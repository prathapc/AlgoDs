1. Print 2d array
Arrays.deepToString(intervals))

2. Sort intervals by start time in asc
Arrays.sort(intervals, (int[] i1, int[] i2) -> i1[0]-i2[0]);

3. string replace vs relaceAll methods
both replaces all occurences. replaceAll is for regex.

4.
